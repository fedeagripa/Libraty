$(document).ready ->
  $("div#new_comment").on("ajax:success", (e, data, status, xhr) ->
    $("div#new_comment").append xhr.responseText
  ).on "ajax:error", (e, xhr, status, error) ->
    $("div#new_comment").append "<p>ERROR</p>"